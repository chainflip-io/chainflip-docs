---

title: Get Chains

description: This method returns the list of available chains or destination chains for the provided sourceChain.

---

# Get Chains

## getChains

Returns the list of available chains or destination chains for the provided `sourceChain`.

```typescript copy
getChains(sourceChain?: Chain): Promise<ChainData[]>
```

{
  <table>
   <thead>
      <tr>
         <th width="223">Param</th>
         <th width="372">Description</th>
         <th width="244.33333333333331">Data type</th>
      </tr>
   </thead>
   <tbody>
      <tr>
         <td><code>sourceChain</code><span class="param-optional-label">(optional)</span></td>
         <td>Chain ID that allows to filter the returned chains</td>
         <td><code>Chain</code></td>
      </tr>
   </tbody>
</table>
}

### Example
If no `sourceChain` is provided, it returns an array of supported chains.
```typescript copy
console.log(await swapSDK.getChains());
```

### Sample Response

```json
[
  {
    "chain": "Ethereum",
    "evmChainId": 1,
    "isMainnet": true,
    "maxRetryDurationBlocks": 10,
    "name": "Ethereum",
    "requiredBlockConfirmations": 2
  },
  {
    "chain": "Polkadot",
    "evmChainId": null,
    "isMainnet": true,
    "maxRetryDurationBlocks": 20,
    "name": "Polkadot",
    "requiredBlockConfirmations": null
  },
  {
    "chain": "Bitcoin",
    "evmChainId": null,
    "isMainnet": true,
    "maxRetryDurationBlocks": 30,
    "name": "Bitcoin",
    "requiredBlockConfirmations": 3
  }
]
```
### Example
If a `sourceChain` is provided, it returns the supported swap destination chains for that specific source chain.
```typescript copy
import { Chains } from "@chainflip/sdk/swap";

console.log(await swapSDK.getChains(Chains.Polkadot));
```

### Sample Response

```json
[
  {
    "chain": "Ethereum",
    "evmChainId": 1,
    "isMainnet": true,
    "maxRetryDurationBlocks": 10,
    "name": "Ethereum",
    "requiredBlockConfirmations": 2
  },
  {
    "chain": "Polkadot",
    "evmChainId": null,
    "isMainnet": true,
    "maxRetryDurationBlocks": 20,
    "name": "Polkadot",
    "requiredBlockConfirmations": null
  }
]
```